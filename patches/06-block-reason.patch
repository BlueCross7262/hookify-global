diff --git a/core/rule_engine.py b/core/rule_engine.py
index f090f0b..8deade0 100644
--- a/core/rule_engine.py
+++ b/core/rule_engine.py
@@ -62,23 +62,26 @@ class RuleEngine:
             messages = [f"**[{r.name}]**\n{r.message}" for r in blocking_rules]
             combined_message = "\n\n".join(messages)
 
-            # Use appropriate blocking format based on event type
-            if hook_event == 'Stop':
+            # 차단 응답을 이벤트별로 분기한다 (공식 스키마 준수).
+            if hook_event == 'PreToolUse':
+                # PreToolUse는 permissionDecisionReason으로 사유를 전달한다.
                 return {
-                    "decision": "block",
-                    "reason": combined_message,
+                    "hookSpecificOutput": {
+                        "hookEventName": "PreToolUse",
+                        "permissionDecision": "deny",
+                        "permissionDecisionReason": combined_message
+                    },
                     "systemMessage": combined_message
                 }
-            elif hook_event in ['PreToolUse', 'PostToolUse']:
+            elif hook_event in ['PostToolUse', 'Stop', 'UserPromptSubmit']:
+                # 나머지 차단 이벤트는 top-level decision/reason을 사용한다.
                 return {
-                    "hookSpecificOutput": {
-                        "hookEventName": hook_event,
-                        "permissionDecision": "deny"
-                    },
+                    "decision": "block",
+                    "reason": combined_message,
                     "systemMessage": combined_message
                 }
             else:
-                # For other events, just show message
+                # 그 외 이벤트는 메시지만 표시한다.
                 return {
                     "systemMessage": combined_message
                 }
